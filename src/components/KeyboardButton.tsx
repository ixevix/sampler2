import React from 'react'

const showKeyboard = () => {

}

const KeyboardButton = () => {
  return (
    <input className='SampleButton' type='button' onClick={showKeyboard} value='Keyboard' />
  )
}

export default KeyboardButton
